# Future plan: PC install-readiness polling — implementation steps

**Why it matters:** For PC (WINDOWS.DESKTOP) streaming playtests, content is *not* pre-pulled the way
Xbox content is. The `PlaytestTitleIngestionWorkflow` must confirm the **exact ingested version** is
installed on a PC server before it declares the title ready. This plan tracks the actual steps to land
that, what is already implemented, and the upstream prerequisites Timi Bolaji called out.

## PR / branch
- **Repo:** services.contentingestion (Xbox.Streaming)
- **Branch:** `t-melanichen/playtest-pc-install-polling` (latest commit `0fab4be6`)
- **PR:** [15896502](https://dev.azure.com/microsoft/Xbox.Streaming/_git/services.contentingestion/pullrequest/15896502) (Draft) — see [PRProgress 17](../PRProgress/17-CTIN-15896502-pc-install-readiness-polling.md).
- **ADO:** task *Implement PC Polling* under work item **62492680** *[XC3] Implement PlaytestTitleIngestionWorkflow*; in-code TODO link **62521491**.
- **Source:** `Transcripts/XCloudIngestion.docx` (Melanie · Timi Bolaji · Jack Heuberger, 2026-06-11). See also [`Blockers/pc-install-readiness-poll.md`](../Blockers/pc-install-readiness-poll.md).

## What "PC polling" does (Timi's design)
PC content lands on demand, so:
1. Adding the title to the offering **triggers** the distribution service to install at least one copy on a PC server.
2. Because every playtest ingestion can be a **new version**, "is this game installed somewhere" is not enough — we must confirm the **latest** version. On a PC server the version is the **content hash** (SHA-256 over concatenated game + DLC versions).
3. To get that hash you do a **resolution** call (resolution API in ingestion) → install id + hash.
4. Then query the **PC Orchestrator** server pool with a content-file filter (install id + hash) and wait until a server reports it. When it shows up, it's ready to play on that version.

---

## Steps

### A. Already implemented in the PR
- [x] **A1 — Resolve the ingested version.** `PollPcFirstInstallAsync` calls `IResolutionProcessor.ResolveContentInstallMetadataAsync` to get the install id + per-version **hash**, scoped to the playtest sandbox + DNA-group flights + `ServerType.PC`. *(Timi: "you need to do a resolution call … now you have your install ID and you have your hash.")* — **PR** `t-melanichen/playtest-pc-install-polling`.
- [x] **A2 — Version-aware PC server query (DONE — now uses PC Orchestrator).** Implemented: the PC branch calls `IPCOrchestratorClient.QueryServersPagedAsync(new FilteredQuery<GameStreamingServerFilter>(filter))` with `filter.Content = [ new ContentFileFilter { Id = targetVersion.InstallId, Version = targetVersion.Hash } ]` (Timi's "content file filter" = per-version install id + hash). Replaces the earlier (wrong) `IServerAllocatorClient` call; the Xbox branch still uses the allocator. Full solution builds clean (0/0); 14/14 workflow unit tests pass. *(Timi: "put that in the game Streaming Server filter and query for servers and wait until something shows up.")*
- [x] **A3 — Retry / terminal wiring.** Empty result → `Retry` on `FirstInstallPollingPolicy` with `NotifyInstallNotFoundAsync` fallback; server found → `NotifyReadyAsync`. Mirrors the Xbox branch. — **PR**.
- [x] **A4 — DI + tests.** Injected `IResolutionProcessor`; 3 PC unit tests (installed→ready, not-installed→retry, version-unresolved→retry) + replaced the old skip test. Build clean, 14/14 pass. — **PR**.

### B. Remaining in this repo (finish the PR)
- [x] **B0 — Reference + wire the PC Orchestrator client (DONE).** Added PackageReferences (Orchestrator.Client/Contracts + Shared.Contracts, v1.0.2606.401) to `Directory.Packages.props` + `ContentCatalog.Ingestion.Core.csproj`; registered `AddGSHttpClient<IPCOrchestratorClient, PCOrchestratorClient>()` in `ContentCatalog.Ingestion.Worker/Startup.cs`; injected `IPCOrchestratorClient` into `PlaytestTitleIngestionWorkflow`. (The `xboxAllocatorClient` field still serves the Xbox branch — rename still optional.)
- [x] **B1 — PC-playtest SUG (wired; value = `PC_PLAYTEST`).** The PC readiness query sets `GameStreamingServerFilter.SystemUpdateGroups` from config `IngestionWorkflowSettings.PlaytestPcReadinessQuery.SystemUpdateGroup` (constrains to the distinct PC playtest SUG when set; unset = current behavior); set to `PC_PLAYTEST` in Int + Test. The SUG itself is now **self-registerable** via the PC SUG Definitions page inheriting `PC_TAKEHOME` — see [`./pc-playtest-dynamic-config-and-sug-setup.md`](./pc-playtest-dynamic-config-and-sug-setup.md). *(Timi: "we will have a distinct PC underscore play test SUG.")* — **PR**.
- [x] **B2 — Region: not filtered (Timi review, DONE — commit `0fab4be6`).** Per Timi's PR feedback the PC query no longer filters by region — it uses a **"first region" assumption** (a server in any region reporting the exact ingested version is sufficient). Removed `PlaytestPcReadinessQuery.Regions`, its appsettings entries, and the env-provider region fallback. Region targeting deferred until we steer developers to specific regions for performance. *(Timi: "No need to filter the PCOR query by regions … we can work with a first region assumption.")*
- [x] **B2b — PC SKU filter (wired; optional value).** The PC query now reads `PlaytestPcReadinessQuery.Skus` and sets `GameStreamingServerFilter.Skus` when configured (completes Timi's "SKU → filter.Skus" mapping). Optional — leave empty to not constrain by SKU.
- [ ] **B3 — Latest-version correctness.** Confirm resolution returns the *newest* ingested version's hash on a republish (version bump), not a stale one — this is the whole reason PC keys on the hash.
- [ ] **B4 — Paging / readiness semantics.** Confirm `QueryServersPagedAsync` paging is handled correctly: an empty first page must be treated as "not ready yet" (→ retry on `FirstInstallPollingPolicy`), not a premature terminal. Define readiness as "a healthy server matching the exact install id + hash (+ SUG) is present," and rely on the polling policy + `NotifyInstallNotFoundAsync` (E2) to bound the wait rather than polling unbounded.
- [ ] **B5 — Resolution business logic for playtests (needs Jack walkthrough) — NOW ITS OWN PR.** PR‑C review: *"Resolution code needs some special business logic for playtests. Jack can probably walk you through it."* Jack (2026‑06‑24) confirmed content resolution only scopes to **GA** packages by default — playtest packages aren't GA, so the resolution processor must be wired to search **non‑GA** packages for the poll to ever resolve a version. **This is split out of the polling PR into its own PR** (Jack: *"that's definitely out of scope … it should be separate from the polling one"*): branch `t-melanichen/resolution-playtest-no-ga-flight` (commit `bef7fe2b` *"Don't scope playtest resolution package search to the GA flight"*). Timi owns the full picture. The poll keys on `version.IsCurrent` (`IsCurrent => AvailableFrom == null`); confirm with Jack/Timi whether a freshly‑ingested playtest version is current/resolvable immediately under the playtest's flights + sandbox and which resolver owns the change. **Do not change the shared resolver speculatively.** Overlaps with the explicit‑version pin (62521491).
- [ ] **B5a — INTEGRATION (rubber‑duck 2026‑06‑24, blocking after merge): wire `IsPlaytest = true` into the poll's `ResolutionQuery`.** PR2 (`resolution-playtest-no-ga-flight`) adds `ResolutionQuery.IsPlaytest` (default **false**), and only skips folding in the GA flight when it's true. But PR1's `PollPcFirstInstallAsync` builds its `ResolutionQuery` with only `Flights`/`SandboxId`/`ServerTypes` (`PlaytestTitleIngestionWorkflow.cs:287‑292`) — it does **not** set `IsPlaytest`. The two are separate branches off main, so today neither compiles against the other. **After both merge, the poll's resolution call must set `IsPlaytest = true` (+ a unit test), or PR2 has no effect on the poll** and the poll resolves with GA‑folded search. Also confirm the Content Targets / distribution resolution path scopes playtests the same way, so CTIN polls for the **same** hash distribution stages.
- [ ] **B5b — Offering attach is a *PR*, not a live write — but the poll starts immediately (rubber‑duck).** `ConfigureOfferingAsync` calls `partnerRegistryClient.ConfigurePlaytestAsync` (→ `BulkEditAsync` creates a **Git PR**) then transitions straight to `PollPcFirstInstallAsync` (`PlaytestTitleIngestionWorkflow.cs:219‑227`). Content Targets won't map the install (→ no PC server install) until that offering PR is **merged**, yet `FirstInstallPollingPolicy` Timeout is **6h** (`appsettings.json`). If manual approval lags (design doc notes ~48h), the poll times out → false `NotifyInstallNotFound`. Ties to [`../Blockers/manual-pr-polling.md`](../Blockers/manual-pr-polling.md). Decide: gate the poll on the offering PR being merged/observed, or extend/separate the approval wait.
- [ ] **B5c — Require a *healthy/usable* server in the orchestrator filter (rubber‑duck).** The poll filter sets only SUG/SKU/Content; `GameStreamingServerFilter` also supports health/state (`IsHealthy`, `IsDeleted`). Consider `IsHealthy = true` (+ ready state, `IsDeleted = false`) so readiness isn't confirmed against an unhealthy/deleted server.
- [ ] **B5d — Resolve can *throw* on read lag instead of retrying (rubber‑duck).** If Catalog replication lag exceeds `ResolutionReadDelay`, `ResolveContentInstallMetadataAsync` can throw "package/version not visible" (`ResolutionProcessor.cs:228‑233`, `CosmosContentCatalog.cs:304‑306`) and fault the stage instead of taking the intended retry/`NotifyInstallNotFound` path. Catch the expected not‑visible cases and route them to the same retry fallback used for "no current PC version yet."

### B′. Rubber‑duck review follow‑ups (2026‑06‑22, both NON‑BLOCKING; no blocking issues found)
- [ ] **B6 — One‑time pre‑resolution delay.** `ResolutionReadDelay` (commit `9975648d`) runs before *every* poll iteration, not just the first. Negligible at 5s vs the 6h/10m policy, but if the delay is increased later the polling wall‑clock drifts from `FirstInstallPollingPolicy.Timeout`. Optional fix: apply it once (on the transition into `PollPcFirstInstallAsync`, or via a job‑state "delay applied" flag). Accepted as‑is for v1.
- [ ] **B7 — Fail fast on *multiple* current PC versions.** `TryGetCurrentVersion` returns false for both zero (transient → retry, correct) and multiple current versions (a should‑never‑happen data‑invariant violation). Retrying the multiple case until `NotifyInstallNotFound` is misleading. Reviewer suggests distinguishing: retry zero, fail fast on multiple. **Note:** this aligns with Timi's original `GetCurrentVersion` steer (which throws on multiple via `SingleOrDefault`) — confirm the desired behavior with Timi before changing, since he owns the resolution/version‑selection semantics.
- [x] **B8 — SUG must never be null → make the PC‑playtest SUG a constant (Jack 2026‑06‑24). DONE — commit `2bffc05c`.** Reviewing `PollPcFirstInstallAsync`, Jack flagged that `GameStreamingServerFilter.SystemUpdateGroups` resolved to `null` when `PlaytestPcReadinessQuery.SystemUpdateGroup` was unset (`pcQuery?.SystemUpdateGroup is { } x ? [x] : null`). The distinct PC‑playtest SUG is fixed for this lane — *"there's no reason we should ever pass in a null … these should be constant."* **Fix:** removed the nullable `SystemUpdateGroup` config and now always scope the query to a code constant `PcPlaytestSystemUpdateGroup = (Id)"PC_PLAYTEST"`; updated appsettings (Int/Test) + the binding/workflow unit tests (24/24 pass). The env‑specific SKU (NC64 vs NC8) stays config‑driven — Jack pointed only at the subgroups; confirm against his written comment. *(Local commit — push + re‑request review.)*

### C. Upstream prerequisites (content-targets / content-install — required for the poll to ever succeed)

> **Pivoted to dynamic config (2026-06-22).** Per Timi's review, the enable + quota settings are applied through **Content Targets dynamic config** (`CONTENTTARGETS/DEFAULT/SERVERSETSCONFIGURATION`), **not** checked-in `appsettings`. services.contenttargets PR [15946980](https://dev.azure.com/microsoft/Xbox.Streaming/_git/services.contenttargets/pullrequest/15946980) reverted its appsettings and is retained tests-only. Full mechanism + copy/paste values: [`./content-targets-pc-playtest-enablement.md`](./content-targets-pc-playtest-enablement.md) and [`./pc-playtest-dynamic-config-and-sug-setup.md`](./pc-playtest-dynamic-config-and-sug-setup.md).

- [x] **C1 — Enable install-on-attach for the PC_playtest SUG (via dynamic config).** As-built, "install-on-attach" = including PC predictions (1 per install id) for the SUG, which is off by default. If an override is still needed, it goes in dynamic config `CONTENTTARGETS/DEFAULT/RESOLUTIONCONFIGURATION` as `IncludePredictions.Override = { Value:true, ServerType:PC, Sugs:[PC_PLAYTEST] }` — **not** appsettings. **Open:** does Int/Test even need this, or are PC predictions already on for existing non-prod PC SUGs? Partner registry now sets `PC_PLAYTEST` on the offering (done); the SUG is self-registerable via the PcSugDefinitions page. *(Timi: "we already have that implemented today. I just have it disabled … for playtest we can conditionally enable it.")*
- [x] **C2 — Set PC server quota ≥ 1 for that SUG (via dynamic config).** Add only `SkuConfigs:STANDARD_NC64AS_T4_V3:QuotasBySugByRegion:WESTUS2:PC_PLAYTEST = 1` under `CONTENTTARGETS/DEFAULT/SERVERSETSCONFIGURATION` (do not clobber the existing live SKU block: `GameplaySlotsPerServer = 4`, `DefaultMaxLocalSpaceInMB = 2000000`). **Still needs:** Timi to apply the dynamic config (or grant access), and the SKU/region capacity to exist (NC64 vs NC8 decision — see setup doc). *(Timi: "we have a quota configuration … we can just set that to one. It's one T4 … it's a dynamic config.")*

### D. Concurrency / queue experience (Timi's separate callout)
- [ ] **D1 — Decide conflict vs. queue for concurrent same-playtest ingestion.** `PlaytestTitleIngestionWorkflow` is `[ExclusiveWorkflow]` with lock id = playtest id and default queue depth **1**, so a second ingestion for the same playtest (new version while one is in flight) currently **conflicts**. Decide whether to keep the conflict experience or raise the queue depth to serialize 1.0 → 1.1. *(Timi: "do you want a conflict experience … or do you want it queued.")* Stretch goal — not required for MVP.

### E. Validation & ship
- [ ] **E1 — End-to-end test:** PC playtest ingest → offering+title attach → distribution installs → resolution returns hash → poll observes the server → workflow reaches `NotifyReadyAsync`. Then republish a new version and confirm it waits for the **new** hash.
- [ ] **E2 — Timeout path:** confirm `NotifyInstallNotFoundAsync` fires if the install never appears within the policy window.
- [ ] **E3 — PR review + merge** (loop in Timi / Jack); add a PRProgress entry.
- [ ] **E4 — Deploy via CI/CICD** (not the PR pipeline) and verify in test.
- [ ] **E5 — Local validation scope (Jack 2026‑06‑24): you cannot test the SAGE green→corp hop locally.** The authorized
  identity is the **Azure managed identity bound to Partner Center**, not the local Partner Center app, so a locally‑run
  Partner Center can't authenticate to the deployed SAGE endpoint. *(Jack: "the thing that's authorized is the managed
  identity in Azure that's connected to Partner Center, not the Partner Center app.")* Instead: (a) **proxy through Dev API**
  (replace the SAGE URL with the Dev API one — already confirmed local→Dev API→content ingestion works), and (b) validate
  that **Partner Center builds the correct job parameters**. The full green→corp SAGE path is only testable once deployed to
  staging; if it fails there it's the green↔corp connection, not the workflow code.

### F. Optional follow-up
- [ ] **F1 — Worker parity.** `TitleIngestionWorker.cs` still uses Xbox-style polling; give it the same PC treatment if that path is also exercised.

## Owners
Melanie Chen (xPlaytest / ingestion workflow) · Timi Bolaji (content-install: C1, C2) · Jack Heuberger.
